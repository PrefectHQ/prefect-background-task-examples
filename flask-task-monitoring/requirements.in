git+https://github.com/prefecthq/prefect.git@main
flask[async]
httpx
marvin
playsound

# On Ubuntu, the pygobject package is a nice way
# sudo apt install libcairo2-dev libgirepository1.0-dev
# pip install pygobject
